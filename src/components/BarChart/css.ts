import { css } from 'emotion';

export const container = css`
  background: #fff;
  position: relative;
  width: 100%;
  height: 100%;

  .xAxis path {
    stroke: #0001;
  }
  .yAxis path {
    stroke: #0001;
  }
  .tick line {
    stroke: #0001;
  }
`;
